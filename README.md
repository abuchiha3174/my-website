# Portfolio README
Edit data.js to update content
Run: python3 -m http.server 8000
Deploy to GitHub Pages per DEPLOY.txt


to run it 
python3 -c "from livereload import Server; s = Server(); s.watch('.'); s.serve(port=8000)"